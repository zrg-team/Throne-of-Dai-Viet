node_modules/
.expo/
dist/
android/
ios/

# Written by `yarn sync` out of the repository root's dist-shell/. A build artifact of a build
# artifact — never source, and 4.9 MB of it.
assets/web.zip
assets/web-version.json

# Copied from public/ by `yarn sync` — the drum has one source, in scripts/build-icon.mjs.
assets/icon.png
assets/adaptive-icon.png
assets/splash.png
